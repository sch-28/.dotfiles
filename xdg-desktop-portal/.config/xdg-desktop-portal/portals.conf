[preferred]
# Only gtk: it works on i3/X11 (FileChooser, Settings, Notification, ...).
# kde/cosmic backends are NOT listed — they need KWin / cosmic-comp and
# SIGABRT-crash when activated under i3. Screenshot/ScreenCast portals have no
# working provider on bare i3/X11 anyway; flameshot uses its own X11 grab.
default=gtk
